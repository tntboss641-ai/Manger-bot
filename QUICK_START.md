# 🚀 دليل البدء السريع

## خطوات النشر في 5 دقائق

### 1️⃣ إنشاء مستودع على GitHub

1. اذهب إلى [github.com/new](https://github.com/new)
2. اسم المستودع: `alliff-bot-manager` (أو أي اسم تريده)
3. اختر Public أو Private
4. **لا تضع علامة** على "Add a README file"
5. اضغط **Create repository**

### 2️⃣ رفع الكود إلى GitHub

**الطريقة الأولى: استخدام السكريبت التلقائي**

```bash
cd /path/to/bot_manager
./deploy_to_github.sh
```

**الطريقة الثانية: الأوامر اليدوية**

```bash
cd /path/to/bot_manager

# ربط المشروع بـ GitHub (استبدل YOUR_USERNAME واسم المستودع)
git remote add origin https://github.com/YOUR_USERNAME/alliff-bot-manager.git

# رفع الكود
git push -u origin main
```

> **ملاحظة**: عند طلب كلمة المرور، استخدم **Personal Access Token** وليس كلمة المرور العادية
> 
> لإنشاء Token: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token
> 
> اختر صلاحية `repo` على الأقل

### 3️⃣ النشر على Back4App

1. اذهب إلى [back4app.com](https://www.back4app.com/) وسجل دخول
2. اضغط **Build new app**
3. اختر **Container as a Service**
4. اختر **Deploy from GitHub**
5. اربط حساب GitHub وامنح الصلاحيات
6. اختر المستودع `alliff-bot-manager`
7. إعدادات النشر:
   - **Branch**: `main`
   - **Port**: `21047` (سيتم تعيينه تلقائياً)
8. اضغط **Deploy** وانتظر 5-10 دقائق

### 4️⃣ الوصول إلى التطبيق

بعد النشر الناجح، ستحصل على رابط مثل:
```
https://alliff-bot-manager-xxxxx.back4app.io
```

**بيانات تسجيل الدخول الافتراضية:**
- اسم المستخدم: `AlliFF121`
- كلمة المرور: `123123123`

---

## 🔄 تحديث التطبيق

عند إجراء تعديلات:

```bash
git add .
git commit -m "وصف التعديل"
git push origin main
```

سيتم إعادة النشر تلقائياً على Back4App.

---

## ⚠️ تنبيهات مهمة

1. **غيّر بيانات الأدمن فوراً** بعد أول تسجيل دخول
2. **الخطة المجانية محدودة**: 
   - 512 MB RAM
   - وقت تشغيل محدود شهرياً
   - قد يدخل في وضع السكون بعد فترة من عدم النشاط
3. **البيانات المحلية قد تُفقد** عند إعادة النشر (استخدم قاعدة بيانات خارجية للحفاظ على البيانات)

---

## 📖 للمزيد من التفاصيل

راجع ملف [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md) للحصول على شرح مفصل وحلول للمشاكل الشائعة.

---

**بالتوفيق! 🎉**
