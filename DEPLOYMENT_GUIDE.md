# 🚀 دليل النشر على GitHub و Back4App

هذا الدليل يشرح خطوة بخطوة كيفية رفع مشروع AlliFF Bot Manager على GitHub ثم نشره على Back4App للحصول على استضافة دائمة ومجانية.

---

## 📋 المتطلبات الأساسية

1. **حساب GitHub**: إذا لم يكن لديك حساب، أنشئ واحد على [github.com](https://github.com/signup)
2. **حساب Back4App**: أنشئ حساب مجاني على [back4app.com](https://www.back4app.com/)
3. **Git مثبت على جهازك**: تحميل من [git-scm.com](https://git-scm.com/downloads)

---

## 🔧 الجزء الأول: رفع المشروع على GitHub

### الخطوة 1: إنشاء مستودع جديد على GitHub

1. سجل دخول إلى حسابك على GitHub
2. اضغط على زر **"+"** في الزاوية العلوية اليمنى واختر **"New repository"**
3. املأ البيانات التالية:
   - **Repository name**: `alliff-bot-manager` (أو أي اسم تفضله)
   - **Description**: `نظام استضافة متخصص لبوتات التليجرام`
   - **Public/Private**: اختر **Public** (عام) أو **Private** (خاص)
   - **لا تضع علامة** على "Initialize this repository with a README"
4. اضغط على **"Create repository"**

### الخطوة 2: رفع الكود إلى GitHub

افتح Terminal أو Command Prompt في مجلد المشروع وقم بتنفيذ الأوامر التالية:

```bash
# الانتقال إلى مجلد المشروع
cd /path/to/bot_manager

# تهيئة Git
git init

# إضافة جميع الملفات
git add .

# إنشاء أول commit
git commit -m "Initial commit: AlliFF Bot Manager"

# ربط المشروع بـ GitHub (استبدل YOUR_USERNAME واسم المستودع)
git remote add origin https://github.com/YOUR_USERNAME/alliff-bot-manager.git

# رفع الكود إلى GitHub
git branch -M main
git push -u origin main
```

**ملاحظة**: عند طلب اسم المستخدم وكلمة المرور:
- استخدم اسم المستخدم الخاص بـ GitHub
- استخدم **Personal Access Token** بدلاً من كلمة المرور العادية
  - لإنشاء Token: Settings → Developer settings → Personal access tokens → Generate new token

---

## 🌐 الجزء الثاني: النشر على Back4App

### الخطوة 1: إنشاء تطبيق جديد على Back4App

1. سجل دخول إلى [back4app.com](https://www.back4app.com/)
2. من لوحة التحكم، اضغط على **"Build new app"**
3. اختر **"Container as a Service"**
4. اختر **"Deploy from GitHub"**

### الخطوة 2: ربط حساب GitHub

1. اضغط على **"Connect to GitHub"**
2. قم بتسجيل الدخول إلى GitHub وامنح Back4App الصلاحيات المطلوبة
3. اختر المستودع `alliff-bot-manager` من القائمة

### الخطوة 3: إعداد التطبيق

1. **App Name**: اختر اسماً للتطبيق (مثل: `alliff-bot-manager`)
2. **Region**: اختر المنطقة الأقرب لك
3. **Branch**: اختر `main`
4. **Build Settings**:
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `cd host && python3 bot.py`
   - أو استخدم الـ Procfile الموجود تلقائياً

### الخطوة 4: إعداد متغيرات البيئة (Environment Variables)

في قسم **Environment Variables**، أضف:

```
PORT=21047
```

### الخطوة 5: النشر

1. اضغط على **"Deploy"**
2. انتظر حتى يكتمل البناء والنشر (قد يستغرق 5-10 دقائق)
3. بعد النشر الناجح، ستحصل على رابط التطبيق مثل:
   ```
   https://alliff-bot-manager-xxxxx.back4app.io
   ```

---

## ✅ التحقق من التشغيل

1. افتح الرابط الذي حصلت عليه من Back4App
2. يجب أن تظهر لك صفحة تسجيل الدخول
3. استخدم بيانات الأدمن الافتراضية:
   - **اسم المستخدم**: `AlliFF121`
   - **كلمة المرور**: `123123123`

---

## 🔄 تحديث التطبيق

عند إجراء تعديلات على الكود:

```bash
# إضافة التعديلات
git add .

# إنشاء commit جديد
git commit -m "وصف التعديلات"

# رفع التعديلات إلى GitHub
git push origin main
```

سيقوم Back4App تلقائياً بإعادة النشر عند رصد تغييرات في المستودع.

---

## ⚠️ ملاحظات مهمة

### 1. حدود الخطة المجانية في Back4App

- **وقت التشغيل**: محدود شهرياً
- **الذاكرة**: 512 MB RAM
- **التخزين**: محدود
- **السكون التلقائي**: قد يدخل التطبيق في وضع السكون بعد فترة من عدم النشاط

### 2. الحفاظ على البيانات

البيانات المخزنة في الملفات (مثل `users.json`, `database.json`) قد تُفقد عند إعادة النشر. للحفاظ على البيانات:

- استخدم قاعدة بيانات خارجية (مثل MongoDB Atlas المجاني)
- أو استخدم خدمة تخزين خارجية (مثل AWS S3)

### 3. الأمان

- **غيّر بيانات الأدمن الافتراضية** فوراً بعد النشر
- لا ترفع ملفات تحتوي على معلومات حساسة إلى GitHub العام
- استخدم متغيرات البيئة للمعلومات الحساسة

### 4. المنافذ (Ports)

- Back4App يستخدم متغير البيئة `PORT` تلقائياً
- تم تعديل الكود ليقرأ من `PORT` أولاً ثم `SERVER_PORT`

---

## 🆘 استكشاف الأخطاء

### التطبيق لا يعمل بعد النشر

1. **تحقق من السجلات (Logs)** في لوحة تحكم Back4App
2. **تأكد من تثبيت المكتبات** بشكل صحيح في `requirements.txt`
3. **تحقق من متغيرات البيئة** المطلوبة

### خطأ في البناء (Build Error)

1. تحقق من ملف `requirements.txt`
2. تأكد من توافق إصدارات المكتبات
3. راجع ملف `runtime.txt` للتأكد من إصدار Python

### التطبيق يعمل لكن لا يمكن الوصول إليه

1. تحقق من إعدادات الشبكة في Back4App
2. تأكد من أن التطبيق يستمع على `0.0.0.0` وليس `localhost`

---

## 📞 الدعم

للمزيد من المساعدة:
- **GitHub Issues**: أنشئ issue في المستودع
- **Back4App Docs**: [docs.back4app.com](https://docs.back4app.com/)
- **تيليجرام**: `@AlliFF_BOT_V1`

---

**بالتوفيق! 🎉**
